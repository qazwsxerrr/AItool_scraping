# Copy this file to .env and fill real X/Twitter credentials locally.
# Do not commit real credentials.
TWITTER_AUTH_TOKEN=your_x_auth_token

# Optional, if X requires a proxy from the RSSHub container.
# PROXY_URI=http://host.docker.internal:2080
