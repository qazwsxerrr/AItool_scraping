# Stage C Plan/Commit：已落地改动与当前问题

本文原是「事件草稿重复冲突」的分析与方案。P0 + P1 已按方案落地，旧多轮草稿协议不再保留。下文只记录实际改了什么，以及当前检查结果。

---

## 1. 改了什么

协议从「多轮 save 已落库草稿 → finalize 硬校验」改为 **Plan/Commit**：

```text
只读：list / search / read candidates + history
研究：search_web / attach_search_evidence（按 event_key）
提交：submit_event_plan（覆盖全部 active 的完整方案）
快照：list_plan_snapshot（最近一次校验或已接受方案）
本地：校验 → 强身份归一 → replace_agent_drafts 原子替换 → materialize
```

模型不再分段持久化、也不再多轮挪已落库成员。本地拥有覆盖、唯一归属、强身份合并、family/split、history/publishability guard。

### 1.1 工具与 Prompt

- Prompt：`stage_c_agent_v16`。工作方式写明 Plan/Commit；校验失败必须重提完整方案，禁止局部补丁。
- Agent 版本：`STAGE_C_AGENT_VERSION = stage_c_agent_v9`；契约 `stage_c_events_v9`；聚合模式 `responses_agent_plan_commit_v1`。
- 已删除热路径：`save_event_drafts` / `finalize_event_drafts` / `mark_unresolved` / `list_event_drafts`，以及 `upsert_agent_draft` / `allow_member_reassignment`。
- 进度条：`event_count` 替代 `draft_count`；工具文案改为提交方案 / 读取快照。

### 1.2 身份（P0）

`exact_identity_keys` 不再把「仓库根 URL + 不同 Release」当成同一精确身份：

| 信号 | 动作 |
| --- | --- |
| `github_release:*` / `github_repo:*` | 只认 external，仓库根 URL 不进 exact |
| 普通文章 canonical URL | 仍作 exact，强制同一可发布事件 |
| 仓库根 URL | 仅 `related_identity_hints`，不强制合并 |

精确身份冲突只检查 `candidate` / `needs_review`。`rejected` 可与候选共享 URL/external，不阻断提交。可发布事件若仍共享强身份，本地自动并入同一事件包。

### 1.3 落库

- `replace_agent_drafts`：先删本 session 全部草稿，再按完整方案插入；证据先按旧 `draft_key` 或 `event_key:` claim 前缀重挂。
- `record_agent_evidence` 可在草稿尚未提交时按 `event_key` 记账；提交后再 `reattach_agent_evidence_by_event_keys`。
- 物化使用 **draft.draft_key 作为 event_key**，避免弱仓库首页 URL 把不同计划事件合成一条。
- `upsert_event` **只按 `(build_id, event_key)` 查找**，不再用 URL / external / alias / primary_item 二次合并。别名仍写入；有强 GitHub external 时，仓库根 URL 不进 alias。
- 预算耗尽：回退只使用已接受 plan；未覆盖 active 收成 `needs_review` singleton，未接受的校验失败快照不落库。

### 1.4 已用定向场景验证（venv 内存库）

通过：两 Release 同仓不同 external 三条独立事件；`candidate`+`rejected` 同 URL 两条独立行且 D 只拿 candidate；同文章 URL 的两个 candidate 自动合并；Tavily 证据按 event_key 绑定并挂到物化事件；配置 Tavily 时 needs_review 未搜索会被拒绝；无有效方案时预算耗尽会落到 unresolved；family 无 split_reason 的正常提交会被拒绝；candidate 无 facts 降为 needs_review；reserve 与 active 同强身份会挂上。

仓库测试套件已不再跟踪，以上不是完整回归。

---

## 2. 当前检查结果

### 2.1 预算回退提交未通过校验方案（已修）

`commit_fallback_plan` 现在只从 **已接受** 的 plan 做回退 seed，不再使用 `last_validation.events` 里的未接受方案。

复现场景已验证：模型先提交同 `event_family_key`、无允许 `split_reason` 的拆分方案时，`submit_event_plan` 仍正确拒绝；随后预算耗尽，回退丢弃这份未接受快照，将未覆盖 active 转成 `unresolved-*` 的 `needs_review` singleton，`_validate_committed_drafts` 通过。

正常 `submit_event_plan` 的 family/split 校验不放宽，修复只影响预算回退路径。

### 2.2 已知限制（不是本次回归失败，但还在）

- 一次提交完整方案，事件多时 token 仍大；未做「先骨架再补 facts」两拍。
- 配置了 Tavily 时，每个 `needs_review` 仍必须先有本 session 的搜索尝试才能提交。
- resume 靠 agent session 的 `accepted_plan` 快照 + transcript；`start_agent_session` 在 fingerprint/prompt 变更时仍会 reset 会话状态。
- Stage D 仍用 `record_agent_evidence(draft_key=None)` 写自己的核验证据，这是 D 的会话，不是 C 残留。

---

## 3. 不再适用的旧结论

下面这些已经随 P1 落地而过时，不要再当现状：

- 热路径上的 `save_event_drafts` / `finalize_event_drafts` / `upsert_agent_draft`。
- 「只做 P0 三刀、保留多轮改库」。
- `upsert_event` 按 URL/external 二次合并（这曾把 rejected 覆盖 candidate，已改为只按 event_key）。
